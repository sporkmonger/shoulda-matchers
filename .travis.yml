language: ruby

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.4
  - rbx-19mode
  - jruby-19mode

gemfile:
  - gemfiles/4.0.0.gemfile
  - gemfiles/4.0.1.gemfile
  - gemfiles/4.1.gemfile

sudo: false

script: "bundle exec rake"

matrix:
  allow_failures:
    - rvm: rbx-19mode
    - rvm: jruby-19mode
